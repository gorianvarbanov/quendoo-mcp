# Quendoo MCP Server - Features

## 🔑 API Key Management (NEW!)

### Built-in Tools

Сървърът включва 3 нови tools за управление на API key:

#### 1. `set_quendoo_api_key(api_key: str)`
Задава твоя Quendoo API key и го кешира за 24 часа.

**Пример:**
```
Моля настрой Quendoo API key: 246dcadb1ed8f76dee198dae12370285
```

**Какво прави:**
- Записва API key в `~/.quendoo_api_key_cache.json`
- Обновява `.env` файла автоматично
- Задава 24-часов срок на валидност
- Връща информация за време на изтичане

#### 2. `get_quendoo_api_key_status()`
Проверява статуса на кеширания API key.

**Пример:**
```
Провери статуса на моя API key
```

**Връща:**
```json
{
  "cached": true,
  "valid": true,
  "set_at": "2025-12-30 19:35:58",
  "expires_at": "2025-12-31 19:35:58",
  "time_remaining_hours": 23.99,
  "api_key_preview": "246dcadb1e..."
}
```

#### 3. `cleanup_quendoo_api_key()`
Изтрива кеширания API key и го маркира като коментар в `.env`.

**Пример:**
```
Изчисти моя Quendoo API key
```

**Какво прави:**
- Изтрива `~/.quendoo_api_key_cache.json`
- Коментира `QUENDOO_API_KEY=` реда в `.env`
- Полезно при смяна на API key или за сигурност

---

## 📦 Други Features

### Property Management
- `get_property_settings()` - Настройки на имота
- `get_rooms_details()` - Детайли за стаи

### Availability
- `get_availability()` - Проверка на наличност
- `update_availability()` - Обновяване на наличност

### Bookings
- `get_bookings()` - Списък с резервации
- `get_booking_offers()` - Оферти за резервация
- `ack_booking()` - Потвърждаване на резервация
- `post_room_assignment()` - Назначаване на стая

### Email & Automation
- Email tools - Изпращане на HTML emails
- Voice call tools - Автоматизирани обаждания с български език

---

## 🔧 Техническа Информация

### API Key Cache System

**Cache файл:** `~/.quendoo_api_key_cache.json`

**Формат:**
```json
{
  "api_key": "your_api_key_here",
  "set_at": "2025-12-30T19:35:58",
  "expires_at": "2025-12-31T19:35:58"
}
```

**Fallback механизъм:**
1. Проверява cache файла (24h валидност)
2. Ако няма или е изтекъл → чете от `QUENDOO_API_KEY` environment variable
3. Ако няма и там → хвърля грешка с инструкции

### Автоматично обновяване на .env

Когато използваш `set_quendoo_api_key()`, файлът `.env` се обновява автоматично:

**Преди:**
```bash
# API Keys
EMAIL_API_KEY=SuperSecret123!
```

**След:**
```bash
# API Keys
QUENDOO_API_KEY=246dcadb1ed8f76dee198dae12370285
EMAIL_API_KEY=SuperSecret123!
```

---

## 🚀 Quick Start

1. **Рестартирай Claude Desktop** с новата версия
2. **Настрой API key:**
   ```
   Настрой Quendoo API key: твоят_api_key_тук
   ```
3. **Използвай tools:**
   ```
   Провери наличността за януари 2026
   ```

API key-ът ще се запомни за 24 часа!

---

## 📚 Документация

- [API_KEY_SETUP.md](./API_KEY_SETUP.md) - Пълна документация за API key setup
- [server_simple.py](./server_simple.py) - Опростен код без OAuth
- [api_key_manager.py](./api_key_manager.py) - API key management логика
